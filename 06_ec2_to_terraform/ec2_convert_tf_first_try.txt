write a script in two parts. the first part accepts an AWS EC2 instance ID and saves the describe-instances output in a variable. The second part converts that description into a terraform template

(note this script only handles the Name tag conversion, so I wanted to start over)
